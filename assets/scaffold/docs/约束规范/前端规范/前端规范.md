# 前端规范

Last verified: <填写：YYYY-MM-DD>

> 一篇汇总覆盖框架、代码、风格、自测、运维。某节写长了且有独立维护者时，再拆同目录专文并改本目录 README。跨端测试门禁只在 [测试规范](../测试规范/测试规范.md)；构建/发布共性只在 [构建发布与运维](../工程规范/构建发布与运维.md)；密钥与环境隔离只在 [安全与配置](../工程规范/安全与配置.md)。本节只写前端特有约定与命令。

填写纪律：项目事实必须能在本仓库（依赖声明、锁文件、源码路径、脚本）核实；核不到就写「不适用」或留 `<填写>`，不要从邻项目、本分发仓或网上教程填具体栈。无前端时各节标不适用，不删目录。命令只抄本仓库真实脚本，并写证据路径。

本仓库前端：`<填写：有 / 无。无则下文各节标不适用>`。

## 1. 框架与边界

通用规则：

- 运行时与框架版本以锁文件 / 依赖声明为准，本文只记录已核实版本，不在特性分支顺手升主版本。
- 路由、状态、请求层分属不同边界：页面不拼环境地址、不直接持有未封装的鉴权头。
- 功能地图上的路径名以本仓库路由定义为准。

| 项 | 值 | 证据 |
| --- | --- | --- |
| 运行时 | `<填写：如 Node / 浏览器基线；无则不适用>` | `<填写：.nvmrc / engines / 构建配置路径>` |
| 框架与版本 | `<填写：名称 + 已锁定版本>` | `<填写：package.json / lock 路径>` |
| 应用入口 | `<填写>` | `<填写>` |
| 路由 | `<填写：定义位置、动态段约定>` | `<填写>` |
| 状态 | `<填写：全局 / 页面 / 服务端状态各放哪>` | `<填写>` |
| 请求层 | `<填写：client、拦截器、基地址注入>` | `<填写>` |
| 禁止事项 | `<填写：例如页面内写死环境 URL、绕过请求层 fetch>` | `<填写：现有封装或 lint 依据；无则写约定待落地>` |

目录边界（只列真实目录，不要按想象补齐）：

| 目录 | 职责 | 不放什么 |
| --- | --- | --- |
| `<填写>` |  |  |

## 2. 代码与契约

通用规则：

- 命名、导入、文件组织跟随本仓库已有写法；没有统一写法时先记现状，再决定是否收敛，不在补文档时改代码。
- 用户可见文案与后端原始错误分离；错误码 / 鉴权头只在请求层或约定的契约层翻译一次。
- 与后端的类型、错误码、鉴权方式必须能指到契约或生成物；对不上就标「未核实」，不编造。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 语言与模块系统 | `<填写：语言、模块格式、类型检查>` | `<填写：tsconfig / 构建配置>` |
| 命名 | `<填写：组件、页面、hook、工具、测试文件>` | `<填写：现有样例路径>` |
| 导入 | `<填写：别名、禁止跨层、循环依赖>` | `<填写>` |
| 错误处理 | `<填写：请求失败、业务码、未登录、超时>` | `<填写：错误组件或拦截器路径>` |
| 后端契约 | `<填写：类型来源、错误码表、鉴权头名；无则不适用>` | `<填写：OpenAPI / 生成目录 / 共享类型>` |

## 3. 页面风格

通用规则：颜色、字号、间距优先走主题令牌；页面里的魔法值要能指回令牌或写明例外。每个会拉数的界面有加载、空、错三态。可访问性未声明时，至少保证可键盘操作、表单有标签、焦点可见。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 主题 / 设计令牌 | `<填写：token 文件、主题切换、暗色是否存在>` | `<填写>` |
| 响应式 | `<填写：断点、移动端是否在范围内>` | `<填写：布局或 CSS 变量>` |
| 可访问性底线 | `<填写：焦点、对比度、aria、跳过链；无审计则写未执行>` | `<填写>` |
| 加载态 | `<填写>` | `<填写>` |
| 空态 | `<填写：无数据、无权限、筛选为空>` | `<填写>` |
| 错误态 | `<填写：可重试范围、是否暴露原始报文>` | `<填写>` |

文案：按钮 / 校验 / 空态用本产品语气。没有文案规范就写「跟随现有页面」，并指一个样例路径。

## 4. 自测（前端特有）

门禁、分档、用例字段、正/边/负/权限、隔离与「重跑不算通过」见 [测试规范](../测试规范/测试规范.md)，此处不复制。这里只登记前端命令与产物。没有对应脚本就写「无」或「待实现」，不要在 init 时编造测试实现，也不要把未跑的命令标成已通过。

| 层 | 何时跑 | 命令 | 产物 / 证据 | 通过标准 |
| --- | --- | --- | --- | --- |
| 单元 | 功能做完；完成标准看测试规范 | `<填写：本仓库真实脚本>` | `<填写：报告目录或 CI job>` | 全绿 |
| 端到端 | 跨页关键路径变化 | `<填写或无>` | `<填写>` | 选定剧本全绿 |
| 冒烟 | 缺陷复现与修复；发布前按范围 | `<填写：剧本目录与跑法>` | `<填写>` | 断言失败即失败 |
| 回归 | 重构或合入共享基线 | 全量单元 + 按范围分档的冒烟 / e2e | 同各层证据 | 任一层失败阻塞 |

新增页面 / 路由 / 可复用模块必须同步用例；改页面或接口对接必须改用例。用例索引写法见测试规范，不在本文粘贴源码。

## 5. 运维（前端特有）

发布通道、回滚、健康检查等仓库级流程见 [构建发布与运维](../工程规范/构建发布与运维.md)。本地启动的逐步说明见 [开发指南](../../项目Wiki/开发指南.md)，命令以本文与开发指南互链，不在 Wiki 再写第二套门槛。密钥、环境隔离见 [安全与配置](../工程规范/安全与配置.md)。

| 主题 | 约定 | 证据 |
| --- | --- | --- |
| 构建 | `<填写：脚本、产物目录、环境注入发生在构建时还是运行时>` | `<填写>` |
| 本地启动 | `<填写：端口、代理、是否依赖真实后端或 mock>` | `<填写：启动脚本 / 代理配置>` |
| 部署 | `<填写：环境名、产物去向、是否独立静态托管>` | `<填写：流水线或部署清单；无则不适用>` |
| 发布 | `<填写：与 Git 通道如何对齐、前端单独发布还是随应用>` | `<填写>` |

禁令：`<填写：例如本地禁止连生产 API、禁止把未压缩源地图发到生产>`。没有依据则写「未声明」。
