# 前端规范

Last verified: <填写：YYYY-MM-DD>

> 一篇汇总覆盖脑图里的框架 / 代码 / 风格 / 自测 / 运维叶子。某节写长了、且有独立维护者时，再拆成同目录专文并改本目录 README。测试门禁的跨端共性见 [测试规范](../测试规范/测试规范.md)，本节只写前端特有命令与产物。

本仓库前端：`<填写：有 / 无。无则下文各节标不适用，不要删目录>`。

## 1. 框架

- 运行时与框架版本：`<填写>`
- 目录与模块边界：`<填写：应用入口、路由、状态、请求层放在哪>`
- 禁止事项：`<填写：例如禁止在页面里直接打未经封装的环境地址>`

## 2. 代码

- 语言与模块系统：`<填写>`
- 命名、导入、错误处理：`<填写>`
- 与后端契约：`<填写：类型生成、错误码、鉴权头>`

## 3. 页面风格

- 设计令牌 / 主题：`<填写>`
- 布局与可访问性底线：`<填写>`
- 文案与空态：`<填写>`

## 4. 自测（前端特有）

命令写本仓库真实脚本，不要预填邻项目。

| 层 | 何时跑 | 命令 | 通过标准 |
| --- | --- | --- | --- |
| 单元 | 功能做完；默认全量 | `<填写>` | 全绿 |
| 端到端 | 跨页关键路径变化 | `<填写或写无>` | 选定剧本全绿 |
| 冒烟 | 缺陷复现与修复验证；发布前按范围 | `<填写：剧本目录与跑法>` | 断言失败即失败，证据写回用例 |
| 回归 | 重构或合入共享基线 | 全量单元 + 按范围分档的冒烟 / e2e | 任一层失败阻塞 |

新增页面 / 路由 / 可复用模块必须同步用例；改页面或接口对接必须改用例。

## 5. 运维（前端特有）

| 主题 | 约定 |
| --- | --- |
| 构建 | `<填写：脚本、产物目录、环境注入>` |
| 本地启动 | `<填写：端口、代理、必需的后端或 mock>` |
| 部署 | `<填写：环境、产物去向>` |
| 发布 | `<填写：与 Git 通道如何对齐>` |
