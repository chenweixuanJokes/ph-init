# 后端规范

Last verified: <填写：YYYY-MM-DD>

> 一篇汇总覆盖框架 / 代码风格 / 公共组件 / 自测 / 运维。权限、网关、注册中心、配置中心等只在本仓库真实依赖时填写，不预填具体厂商或环境。测试门禁共性见 [测试规范](../测试规范/测试规范.md)。

本仓库后端：`<填写：有 / 无。无则下文各节标不适用，不要删目录>`。

## 1. 框架

- 语言、运行时、框架版本：`<填写>`
- 分层与模块边界：`<填写：入口、领域、持久化、对外客户端>`
- 代码生成 / 脚手架（若有）：`<填写：哪些文件可手改，哪些是生成物>`

## 2. 代码风格

- 命名、包结构、注释要求：`<填写：注释写 why，不写 what>`
- 校验与错误模型：`<填写>`
- 日志与可观测性底线：`<填写>`

## 3. 依赖的公共组件

只登记本仓库实际调用的共享能力。没有则写“无”。

| 能力 | 本仓库如何用 | 禁令 |
| --- | --- | --- |
| 权限 / 身份 | `<填写或无>` | `<填写>` |
| 网关 / 服务发现 | `<填写或无>` | 本地调试是否允许注册：`<填写>` |
| 配置中心 | `<填写或无>` | `<填写>` |
| 其它 | `<填写或无>` | `<填写>` |

需要调用平台或其它内部服务时：优先用已发布的 SDK / 契约，禁止在业务模块里手写重复客户端和镜像 DTO——除非本仓库明确没有 SDK。

## 4. 自测（后端特有）

| 层 | 何时跑 | 命令 | 通过标准 |
| --- | --- | --- | --- |
| 单元 | 功能做完；默认模块全量 | `<填写>` | 全绿 |
| 接口 | 新增或修改 HTTP / RPC 契约 | `<填写>` | 用例与契约同步 |
| 端到端 | 跨服务关键路径 | `<填写或写无>` | 选定范围全绿 |
| 冒烟 | 缺陷复现与修复验证 | `<填写：用例目录与跑法>` | 同一用例先红后绿 |
| 回归 | 重构或合入共享基线 | 全量单元 + 全量或分档冒烟 | 任一层失败阻塞 |

新增接口必须同步用例；改接口必须改用例。重构不论看起来多等价，都按回归门禁。

## 5. 运维（后端特有）

| 主题 | 约定 |
| --- | --- |
| 构建 | `<填写>` |
| 本地启动 | `<填写：profile / 环境变量；禁止用生产配置启动则写明>` |
| 部署 | `<填写>` |
| 发布 | `<填写：与 Git 通道如何对齐>` |
