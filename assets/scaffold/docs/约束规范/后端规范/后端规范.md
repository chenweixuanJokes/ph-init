# 后端规范

Last verified: <填写：YYYY-MM-DD>

> 一篇汇总覆盖框架、代码、公共组件、自测、运维。跨端测试门禁只在 [测试规范](../测试规范/测试规范.md)；构建/发布共性只在 [构建发布与运维](../工程规范/构建发布与运维.md)；密钥与权限边界只在 [安全与配置](../工程规范/安全与配置.md)。权限服务、网关、注册中心、配置中心分行列出，只在本仓库真实依赖时填写。Kong / Eureka / Apollo 是条件项，不是默认栈。

填写纪律：语言、框架、中间件必须能在依赖声明、构建文件或启动配置里核实。核不到就写「无」或「不适用」，不要预填具体厂商、环境名或邻项目分层。无后端时各节标不适用，不删目录。命令只抄本仓库真实脚本。

本仓库后端：`<填写：有 / 无。无则下文各节标不适用>`。

## 1. 框架与模块

通用规则：入口适配器、领域规则、持久化、对外客户端分层；HTTP / RPC 细节不渗进领域。生成物与手改文件分开，不要改生成物凑功能。

| 项 | 值 | 证据 |
| --- | --- | --- |
| 语言 / 运行时 / 框架版本 | `<填写>` | `<填写：构建文件、锁文件、运行镜像>` |
| 应用入口 | `<填写>` | `<填写>` |
| 模块划分 | `<填写：按域 / 按应用 / 单模块>` | `<填写：源码根或模块清单>` |
| 分层 | `<填写：入口、应用、领域、持久化、对外客户端>` | `<填写>` |
| 代码生成 / 脚手架 | `<填写：可手改 vs 生成物；无则不适用>` | `<填写>` |

| 模块 | 职责 | 代码路径 | 不跨越的边界 |
| --- | --- | --- | --- |
| `<填写>` |  |  |  |

## 2. 代码风格

通用规则：命名与包结构跟仓库现状。注释写 why，不写 what。校验发生在边界；领域对象不接收未校验的外部输入。对外错误有稳定码，不把堆栈或内部 SQL 回给调用方。日志默认脱敏，禁止写密钥、cookie、完整证件号；关联 ID 仅在项目已有该机制时沿用同一套。改对外契约必须改调用方与用例。写操作声明事务边界；跨库 / 跨服务不假装本地事务。可能被重试的写接口要有幂等键或自然幂等。超时与重试只放在出口，有上限和幂等前提，禁止对非幂等写无限重试。Schema 变更走迁移脚本，不改已应用的历史迁移。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 命名与包结构 | `<填写>` | `<填写：样例包路径>` |
| 校验 | `<填写：入参、路径、权限注解>` | `<填写>` |
| 异常 / 错误模型 | `<填写：错误码、HTTP/RPC 映射、是否区分 4xx/5xx>` | `<填写>` |
| 日志与可观测 | `<填写：级别、必带字段、追踪；无则写未建立>` | `<填写>` |
| 接口契约 | `<填写：HTTP/RPC、版本策略、兼容规则、契约存放处>` | `<填写：OpenAPI / proto / 路由清单>` |
| 鉴权 | `<填写：谁验、令牌放哪、匿名接口名单>` | `<填写：过滤器 / 中间件路径>` |
| 事务 | `<填写：默认传播、禁止事项>` | `<填写>` |
| 幂等 | `<填写：哪些写接口、键从哪来>` | `<填写>` |
| 超时与重试 | `<填写：出口超时、重试次数、对何种错误重试>` | `<填写：client 配置>` |
| 持久化 | `<填写：库 / 缓存 / 消息；无则不适用>` | `<填写>` |
| 数据迁移 | `<填写：工具、存放目录、回滚策略>` | `<填写>` |

## 3. 依赖的公共组件

只登记本仓库实际调用的共享能力。没有该能力的行写「无」，不要把表格删掉。需要调用平台或其它内部服务时：优先用已发布 SDK / 契约，禁止在业务模块手写重复客户端和镜像 DTO——除非本仓库明确没有 SDK。

Kong、Eureka、Apollo 以及同类产品：仅当依赖声明或运行配置证明在用时，写入「本仓库如何用」。不得因为常见而预装。

| 能力 | 是否使用 | 本仓库如何用 | 禁令 | 证据 |
| --- | --- | --- | --- | --- |
| 权限 / 身份服务 | `<填写：使用 / 无>` | `<填写或无>` | `<填写>` | `<填写>` |
| 网关 / 服务治理 | `<填写：使用 / 无>` | `<填写：若是 Kong 等须有依赖证据>` | 本地是否必须挂网关：`<填写>` | `<填写>` |
| 注册中心 | `<填写：使用 / 无>` | `<填写：若是 Eureka 等须有依赖证据>` | 本地调试是否允许注册：`<填写>` | `<填写>` |
| 配置中心 | `<填写：使用 / 无>` | `<填写：若是 Apollo 等须有依赖证据>` | 本地覆盖优先级：`<填写>` | `<填写>` |
| 其它 | `<填写或无>` | `<填写>` | `<填写>` | `<填写>` |

配置项只记名称与来源，不把真实值抄进文档；值的保管见 [安全与配置](../工程规范/安全与配置.md)。清单可落在 [开发指南](../../项目Wiki/开发指南.md)。

## 4. 自测（后端特有）

门禁与用例写法见 [测试规范](../测试规范/测试规范.md)，此处不复制。这里只登记后端命令与产物。没有脚本就写「无」或「待实现」；init 不代写测试实现，未执行不得标通过。

| 层 | 何时跑 | 命令 | 产物 / 证据 | 通过标准 |
| --- | --- | --- | --- | --- |
| 单元 | 功能做完；默认模块全量 | `<填写>` | `<填写>` | 全绿 |
| 接口 | 新增或修改 HTTP / RPC 契约 | `<填写>` | `<填写>` | 用例与契约同步 |
| 端到端 | 跨服务关键路径 | `<填写或无>` | `<填写>` | 选定范围全绿 |
| 冒烟 | 缺陷复现与修复验证 | `<填写：用例目录与跑法>` | `<填写>` | 同一用例先红后绿 |
| 回归 | 重构或合入共享基线 | 全量单元 + 全量或分档冒烟 | 同各层证据 | 任一层失败阻塞 |

新增接口必须同步用例；改接口必须改用例。重构不论看起来多等价，都按回归门禁。

## 5. 运维（后端特有）

仓库级构建、部署、回滚、监控见 [构建发布与运维](../工程规范/构建发布与运维.md)。逐步上手见 [开发指南](../../项目Wiki/开发指南.md)。

| 主题 | 约定 | 证据 |
| --- | --- | --- |
| 构建 | `<填写：命令、产物、是否含迁移>` | `<填写>` |
| 本地启动 | `<填写：profile / 环境变量；禁止用生产配置则写明>` | `<填写>` |
| 部署 | `<填写：环境、进程模型、是否多实例>` | `<填写>` |
| 发布 | `<填写：与 Git 通道如何对齐、是否分批>` | `<填写>` |

禁令：`<填写：例如禁止生产库用于本地测试、禁止关闭鉴权启动共享环境>`。没有依据则写「未声明」。
