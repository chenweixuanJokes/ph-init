---
intent_id: "INT-YYYYMMDD-短词"
type: feature
status_dir: 进行中/新特性
created: "YYYY-MM-DD"
updated: "YYYY-MM-DD"
related: []
---

# <标题>

`intent_id` 在移动文件时保持不变。`type` 取 `feature` / `issue` / `interview`。`status_dir` 必须与当前所在目录一致。

## 背景

为什么现在做。关联访谈或问题用仓库相对路径列入 `related`。

## 目标

可验证的完成标准。写“怎样算做成”，不写实现步骤。

## 非目标

明确不做的范围。

## 记录

按时间追加。完成或废弃时在此写一行：日期、目标目录、原因；然后**移动**本文件，并更新沿途 README。
