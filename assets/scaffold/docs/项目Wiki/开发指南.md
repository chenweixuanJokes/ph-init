---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "启动、构建、环境变量或依赖安装命令变化"
---

# 开发指南

日常怎么把本仓库跑起来。命令必须是本仓库真实命令；强制门槛指向约束，不在这里复制长规范。

## 前置

- 运行时：
- 依赖服务：
- 克隆后还要做的一次性步骤：

## 启动

```text
<填写：前端 / 后端各自的启动命令与必要参数>
```

禁止事项（例如禁止用生产配置启动）写在这里，并链到对应约束。

## 验证

开发中先看 [测试规范](../约束规范/测试规范/测试规范.md)。把最常用的一条验证命令抄在下面，避免到两处查找：

```text
<填写：最常用的单元或冒烟命令>
```

## 日常流程

1. 从基线拉特性分支（见 [Git 与并行开发](../约束规范/工程规范/Git与并行开发.md)）
2. 并行任务进入 `.worktrees/`
3. 改代码与对应用例
4. 按门禁验证
5. 合入前更新受影响 Wiki 的 `last_verified`
