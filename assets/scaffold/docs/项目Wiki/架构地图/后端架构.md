---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "后端分层、模块拆分、数据存储或对外集成变化"
---

# 后端架构

本仓库无后端时，把 `status` 改为 `deprecated`，正文写“不适用”。

## 分层

入口、应用、领域、持久化、对外客户端各在哪。

## 集成

只列真实依赖的中间件与下游系统，并链到 [后端规范](../../约束规范/后端规范/后端规范.md) 第 3 节。
